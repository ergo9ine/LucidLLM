# LucidLLM Chat

[한국어](../README.md) | [English](README.en.md) | [日本語](README.ja.md) | [简体中文](README.zh-CN.md)

![License](https://img.shields.io/github/license/ergo9ine/LucidLLM)
![Transformers.js](https://img.shields.io/badge/Transformers.js-v3.8.1-yellow)
![WebGPU](https://img.shields.io/badge/WebGPU-Supported-green)
![PWA](https://img.shields.io/badge/PWA-Planned-blue)

**LucidLLM** 是一款基于 [Transformers.js](https://huggingface.co/docs/transformers.js) 和 WebGPU 技术的聊天应用程序，能够在浏览器内完全本地运行 AI 模型。它采用无需构建（Zero-build）的架构，提供全面的隐私保护，无需将数据发送到外部服务器即可提供强大的 AI 功能。

> **主要特点:** 18,200+ 行代码 • 支持 4 种语言 • WebGPU/WASM 推理加速 • 实时 Token 流式传输 • 自定义虚拟 DOM • AES-256 加密备份 • OPFS 模型缓存

## ✨ 核心功能

### 🤖 AI 与模型

| 功能 | 说明 |
|------|------|
| **完全本地推理** | 所有 AI 运算均在浏览器内执行，数据不会离开您的设备。 |
| **WebGPU 加速** | 支持通过 WebGPU 进行高速推理，在不支持的浏览器中自动回退到 WASM。 |
| **OPFS 模型缓存** | 使用 Origin Private File System 永久存储模型，避免重复下载。 |
| **智能下载** | 支持断点续传、指数退避（Exponential Backoff）重试和量化（Quantization）选择。 |
| **多模型支持** | 可缓存多个 ONNX 模型并自由切换使用。 |
| **模型卡片显示** | 详细显示模型的元数据（上传者、任务、许可证、点赞数等）。 |
| **Hugging Face 集成** | 可直接从 Hugging Face Hub 搜索并下载模型。 |

### 💬 聊天体验

| 功能 | 说明 |
|------|------|
| **多会话聊天** | 可创建多个独立的聊天标签页，分别管理对话记录。 |
| **实时流式传输** | 实时显示 Token 生成过程。 |
| **性能监控** | 实时显示每秒 Token 数 (TPS) 统计（平均/最大/最小）和内存使用情况。 |
| **系统提示词** | 可自定义 AI 助手的角色和行为。 |
| **上下文控制** | 可调节上下文窗口大小，从 4k 到 128k。 |
| **聊天导出** | 可将对话内容作为 JSON 文件备份和导出。 |

### 🔒 隐私与备份

| 功能 | 说明 |
|------|------|
| **Google Drive 备份** | 将设置和聊天记录安全备份到 Google Drive。 |
| **强加密** | 在客户端使用 PBKDF2 密钥派生（250,000 次迭代）和 AES-GCM-256 算法进行加密。 |
| **自动备份** | 数据变更时自动执行备份（应用防抖动）。 |
| **版本管理** | 保留多个备份版本，可随时恢复到任意时间点。 |
| **无服务器通信** | 除明确的备份操作外，任何数据都不会发送到外部服务器。 |

### 🌐 用户体验

| 功能 | 说明 |
|------|------|
| **4 种语言支持** | 支持韩语、英语、日语、简体中文，并自动检测浏览器语言。 |
| **主题选项** | 支持深色模式、浅色模式和 OLED 纯黑模式。 |
| **响应式设计** | 为手机、平板、桌面等所有设备提供优化的 UI。 |
| **快捷键支持** | 提供新聊天 (Ctrl+Shift+N)、导出 (Ctrl+Shift+E)、侧边栏切换 (Ctrl+B) 等提高效率的快捷键。 |

## 📋 系统要求

### 浏览器兼容性

| 要求 | 详细内容 |
|------|----------|
| **推荐浏览器** | Chrome 113+ / Edge 113+ (支持 WebGPU) |
| **最低配置** | 所有支持 WASM 的现代浏览器 |
| **安全上下文** | 使用 OPFS 必须在 HTTPS 或 localhost 环境下 |

### 硬件推荐配置

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| **内存 (RAM)** | 4GB | 8GB 以上 |
| **显卡 (GPU)** | 集成显卡 | 支持 WebGPU 的独立显卡 |
| **存储空间** | 每个模型 100MB ~ 2GB | 推荐使用 SSD |

## 🚀 开始使用

### Web 版本（无需安装）

通过 GitHub Pages 无需安装即可直接使用：

👉 **[运行在线演示](https://ergo9ine.github.io/LucidLLM/)**

### 本地运行

1. **克隆仓库**

```bash
git clone https://github.com/ergo9ine/LucidLLM.git
cd LucidLLM
```

2. **运行本地服务器**

要使用 OPFS 和 Service Worker 功能，需要安全上下文（HTTPS 或 localhost）。

```bash
# 使用 Python
python -m http.server 8000

# 使用 Node.js (npx)
npx serve .

# 或使用包含的 npm 脚本
npm run serve
```

3. **访问浏览器**

在浏览器中访问 `http://localhost:8000`。（推荐使用 Chrome 或 Edge）

## 📖 使用指南

### 1. 加载模型

1. 点击标题栏的 **设置(⚙️)** 按钮。
2. 切换到 **模型管理** 标签页。
3. 输入 Hugging Face 模型 ID（例如：`onnx-community/SmolLM2-135M-Instruct`）。
4. 点击 **查询** 按钮获取模型信息。
5. 选择所需的 **量化 (Quantization)** 选项。
6. 点击 **下载** 将模型缓存到 OPFS。（支持断点续传）
7. 下载完成后，点击 **激活** 按钮加载模型。

### 2. 开始聊天

1. 在底部输入框输入消息，点击 **发送** 按钮或按 `Enter` 键。
2. 点击标签栏的 **+** 按钮创建新的聊天会话。
3. 点击标签可自由切换多个对话会话。

### 3. LLM 设置

在 **设置 > LLM 设置** 中可以调整以下项目：

| 设置 | 默认值 | 说明 |
|------|--------|------|
| **系统提示词** | "You are a helpful assistant." | 定义 AI 的角色和性格。 |
| **最大输出 Token** | 512 | 限制一次回复生成的最大长度。 |
| **上下文窗口** | 8k | 设置模型可以记忆的对话长度。 |
| **温度 (Temperature)** | 0.9 | 调节回复的创造性和随机性。 |

### 4. Google Drive 备份

1. 转到 **设置 > 备份与恢复**。
2. 点击 **连接 Google Drive** 按钮并登录。（自动设置客户端 ID）
3. 启用 **自动备份** 后，每次变更都会自动保存。
4. 点击 **立即备份** 按钮可进行手动备份。
5. 从恢复列表中选择所需的时间点，点击 **恢复** 按钮即可还原到之前的状态。

## 🏗️ 项目结构

```
LucidLLM/
├── index.html                  # 主 HTML 入口点
├── script/
│   ├── bootstrap.js            # 应用程序初始化
│   ├── main.js                 # 核心逻辑及状态管理
│   ├── i18n.js                 # 多语言处理模块
│   ├── shared-utils.js         # 通用工具
│   ├── worker.js               # 推理用 Web Worker
│   └── drive-backup.js         # Google Drive 备份逻辑
├── style.css                   # 样式及主题定义
├── favicon.svg                 # 应用程序图标
└── package.json                # NPM 配置
```

## 🛠️ 技术栈

| 分类 | 技术 |
|------|------|
| **语言** | JavaScript (ES2020+ Modules) |
| **架构** | Zero-build, Vanilla JS (无框架) |
| **ML 框架** | Transformers.js v3.8.1 |
| **推理后端** | WebGPU / WASM (自动切换) |
| **存储** | Origin Private File System (OPFS), localStorage |
| **样式** | Tailwind CSS v3 (CDN) + 自定义 CSS 变量 |
| **图标** | Lucide Icons (CDN) |
| **认证** | Google Identity Services (OAuth 2.0) |
| **加密** | Web Crypto API (PBKDF2, AES-GCM-256) |

## 📄 许可证

本项目基于 **MIT 许可证** 分发。详细信息请参阅 [LICENSE](../LICENSE) 文件。

## 🙏 致谢

- [Hugging Face](https://huggingface.co/) - Transformers.js 及模型托管
- [Transformers.js](https://huggingface.co/docs/transformers.js) - 浏览器内 ML 推理库
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先 CSS 框架
- [Lucide Icons](https://lucide.dev/) - 精美的开源图标
- [Space Grotesk](https://fonts.google.com/specimen/Space+Grotesk) - 字体系列

---

**Made with ❤️ for privacy-focused AI**

[⬆ 回到顶部](#lucidllm-chat)
